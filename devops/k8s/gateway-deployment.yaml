apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-mono-repo-template-api-gateway
  labels:
    app: node-mono-repo-template
spec:
  replicas: 3
  selector:
    matchLabels:
      app: node-mono-repo-template
      tier: api-gateway
  template:
    metadata:
      labels:
        app: node-mono-repo-template
        tier: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: your-docker-repo/node-mono-repo-template-api-gateway:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: secret
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-url
                  key: url
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: node-mono-repo-template-api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: node-mono-repo-template
    tier: api-gateway